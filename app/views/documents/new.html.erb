<div class="col-6 offset-3">
  <p class="text-alert mt-3">Attention ! En plus du dossier qu'on te permet de créer via cette Web-Application il faudra également que tu fournisses à Pôle-Emploi une attestation employeur (de ton ancien employeur) et le certificat d'accomplissement de ton V.I</p>
  <h1 class="text-center">Remplis les infos</h1>
  <%= simple_form_for [@info, @document] do |f| %>
    <%= f.input :first_name, label: "Ton prénom" %>
    <%= f.input :last_name, label: "Ton nom" %>
    <%= f.input :company, label: "La boite dans laquelle tu as effectué ton V.I" %>
    <%= f.input :work, label: "Énoncé de ton poste" %>
    <%= f.input :work_type, collection: Document::WORK_TYPE, label: "Type de contrat" %>
    <%= f.input :start_date, label: "Date à laquelle tu as commencé ton V.I", as: :string, required: false, input_html: {class: "datepicker"} %>
    <%= f.input :end_date, label: "Date à laquelle tu as terminé ton V.I", as: :string, required: false, input_html: {class: "datepicker"} %>
    <%= f.input :old_company, label: "Ton dernier employeur avant ton V.I" %>
    <%= f.input :old_work, label: "Intitulé de ton ancien poste" %>
    <%= f.input :old_start_date, label: "Date de début de ton ancien poste", as: :string, required: false, input_html: {class: "datepicker"} %>
    <%= f.input :old_end_date, label: "Date de fin de ton ancien poste", as: :string, required: false, input_html: {class: "datepicker"}%>
    <% if @info.unemployment == "Oui" %>
      <%= f.input :start_unemployment_at, label: "Date de début de ton chomage", as: :string, required: false, input_html: {class: "datepicker"}%>
    <% end %>
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
      Ajouter un travail
    </button>
    <small> Si besoin, ajouter un ou plusieurs travails (CDD, intérim ..) </small>
    <% unless @info.jobs.empty?  %>
      <div class="container-other-jobs">
        <div class="title-other-jobs">Autre(s) emploi(s) :</div>
        <% @info.jobs.each do |job| %>
          <div class="card-job">
            <%= job.company %> - <%= job.work %> - <%= job.start_at.strftime("%e %B %Y") %> / <%= job.end_at.strftime("%e %B %Y") %>
            <%= link_to info_job_path(@info, job), method: :delete, data: { confirm: "Supprimer ce travail ?" } do %>
              <i class="far fa-trash-alt"></i>
            <% end %>
          </div>
        <% end %>
      </div>
    <% end %>
    <%= f.submit "Gratte ton chômage !", class: "btn btn-primary" %>
  <% end %>
  <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Ancien Travail</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <%= simple_form_for [@info, @job] do |f| %>
            <%= f.input :company, label: "Nom de l'employer" %>
            <%= f.input :work, label: "Intitulé du poste" %>
            <%= f.input :start_at, label: "Date de début du poste", as: :string, required: false, input_html: {class: "datepicker"} %>
            <%= f.input :end_at, label: "Date de fin du poste", as: :string, required: false, input_html: {class: "datepicker"}%>
        </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <%= f.submit "Ajoute ton travail" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>